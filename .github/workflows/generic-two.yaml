name: validation-workflow-two
# Run this workflow every time a commit gets pushed to main or a pull request gets opened against main
on:
  pull_request:
    paths-ignore:
      - 'two/**'

jobs:
  validation-workflow-two:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
